# ESIA Integration Notes

- Treat ESIA as partner-gated by default unless the exact public integration path is documented.
- Keep the agent focused on preparation, required fields, and official handoff.
- Distinguish clearly between `precheck`, `redirect`, and `post-auth`.
- Do not store more profile data than the workflow requires.
