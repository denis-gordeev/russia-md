# Marketplace Integration Notes

- Keep repricing bounded by explicit policy thresholds.
- Prefer draft edits and operator review for listing changes.
- Log before/after snapshots for every mutation.
- Model platform-specific validation rules explicitly rather than assuming they match across sellers.
